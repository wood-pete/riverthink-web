{
  "name": "riverthink-landing",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "copy-blog-images": "node scripts/copy-blog-images.js",
    "dev": "npm run copy-blog-images && next dev",
    "build": "npm run copy-blog-images && next build",
    "export": "npm run copy-blog-images && next build && rm -rf docs && mv out docs && touch docs/.nojekyll",
    "start": "next start"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "next": "^14.2.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "remark": "^15.0.1",
    "remark-html": "^16.0.1"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.1"
  }
}
